# file: playbook-choco-upgrade-openssh.yml
---
- name: Upgrade OpenSSH using Chocolatey
  hosts: all
  tasks:
    - name: Upgrade OpenSSH to latest
      win_chocolatey:
        name: openssh
        package_params: '"/SSHAgentFeature /SSHServerFeature"'
        state: latest